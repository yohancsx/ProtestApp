import 'package:flutter/material.dart';

///Model for the media page
class MediaPageModel extends ChangeNotifier {
  MediaPageModel({@required this.context});

  ///The current build context
  BuildContext context;
}
